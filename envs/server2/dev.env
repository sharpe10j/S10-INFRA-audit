# server2 overrides

# Docker version pins (SEMVER). install_docker.sh resolves apt strings for noble.
DOCKER_SEMVER=28.1.1
CONTAINERD_SEMVER=1.7.27
COMPOSE_SEMVER=2.38.1
BUILDX_SEMVER=0.25.0
APT_HOLD_DOCKER=1

# (Optional) monitoring stack name, if you want to reference it elsewhere
MON_STACK_NAME=s10-monitoring

# Keeper ID for server2
KEEPER_SERVER_ID=2

