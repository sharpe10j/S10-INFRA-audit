# server1 overrides
CH_HOST=${SERVER1_HOST}

# Keeper ID for server1
KEEPER_SERVER_ID=1

# Only server1 installs Node Exporter on the host (no var needed; bootstrap handles it)
# Add any CH-specific overrides here if you diverge from global defaults.
