# server3 overrides

# Docker version pins (SEMVER). Keep in sync with server2.
DOCKER_SEMVER=28.1.1
CONTAINERD_SEMVER=1.7.27
COMPOSE_SEMVER=2.38.1
BUILDX_SEMVER=0.25.0
APT_HOLD_DOCKER=1

# (Optional) if server3 uses different data roots:
# KAFKA_DATA=/mnt/kafka-data
# ZK_DATA=/mnt/zk-data
