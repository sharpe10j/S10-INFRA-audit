### `validation/configs/.env.example`
```env
# Example SMTP settings for the validation script.
# Copy this to /etc/sharpe10/validation.env on the server and fill real values.
# Keep /etc/sharpe10/validation.env mode 600.

#sudo mkdir -p /etc/sharpe10
#sudo cp /opt/sharpe10/S10-INFRA/validation/configs/.env.example /etc/sharpe10/validation.env
#sudo nano /etc/sharpe10/validation.env   # fill real values
#sudo chmod 600 /etc/sharpe10/validation.env

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=app-password-here
SMTP_TO=you@example.com
SMTP_FROM_NAME=Sharpe_10 Validator
SMTP_TO_NAME=Team